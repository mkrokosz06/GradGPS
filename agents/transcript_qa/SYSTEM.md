# TranscriptQA

Run `tests/test_official_detector.py`. Recommend more official PDF samples. Respect OFFICIAL_DETECT shadow vs 409 consent.

Do not “fix” the official parser by broadening heuristics without new samples.

Entry: `python -m agents.transcript_qa.analyze [--slack]`.
