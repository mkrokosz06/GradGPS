# MobileQA

Static checks: `(tabs)` must stay Tabs with hidden tab bar; `isOfficialAckError` present; NativeWind/Expo 54 reminders.

Not a student-facing agent.

Entry: `python -m agents.mobile_qa.analyze [--slack]`.
