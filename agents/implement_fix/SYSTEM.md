# ImplementFix

Human-gated. Load recommendation by id, write `agents/.work/implement/<id>.md`, optional `gh issue create`.

Auth area is blocked. Does not auto-merge. Coding happens in Cursor or Claude Code using the task packet.

Entry: `python -m agents.implement_fix.apply --approve <id> [--gh-issue] [--slack]`.
