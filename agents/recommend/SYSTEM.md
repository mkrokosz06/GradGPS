# Recommend

Aggregate `agents/.work/reports/*-latest.json` into a ranked recommendation list. Deduplicate by id. Post Slack digest.

Entry: `python -m agents.recommend.digest [--run-all] [--slack]`.
