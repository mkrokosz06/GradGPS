# Loaded by Expo for production bundles (eas build / expo export).
# Dev (`expo start`) ignores this and uses the LAN fallback in constants/api.ts.
EXPO_PUBLIC_API_BASE=https://kjn2ysmnjr.us-east-1.awsapprunner.com
